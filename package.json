{
  "name": "ece461l-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "heroku-prebuild": "npm config set include-dev true",
    "heroku-postbuild": "cd ee461l-portal && npm install && npm run build",
    "start": "node ee461l-portal/server.js"
  },
  "engines": {
    "node": "22.x"
  }
}
